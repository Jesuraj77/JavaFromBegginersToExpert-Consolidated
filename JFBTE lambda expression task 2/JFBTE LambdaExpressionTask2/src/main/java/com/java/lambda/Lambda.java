package com.java.lambda;

public class Lambda {
/*
	 * Create an interface with the name "IGreet",
	 *  that contain an abstractmethod sayHello() that takes username as parameter.
	 *  Write the main program, in which provide the implementation for 
	say Hello () using lambda expressions
 */
}
