package com.java.generics;

public class Generics {
/*
		 * Write a Java Program to create a Generic Class with the name "Swapper"
		that contain a static generic method "doSwap()" that takes two arguments of 
		any type and swap the variables and display the values before and
		after swapping,
		Create a super class with the name "Employee" that contain empName and
		designation as properties. Create a sub class "Manager" that contain
		allowance as property. Create a main program that contain a generic
		method
		which takes a wild card to display the details of name and 
		designation.

 */
}
